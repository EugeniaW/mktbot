version: '3.8'

services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./outputs:/app/outputs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
